/*
This method needs to run from frontend context, it returns a boolean of whether the user is logged into the site or not */

// TODO implement this function to check if user is logged in
function isLoggedIn() {
    return false;
}

export default isLoggedIn;
