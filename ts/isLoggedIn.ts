/*
 * This method returns whether the user is logged into the site or not
 * */

// TODO implement this function to check if user is logged in
function isLoggedIn(): boolean {
    return false;
}

export default isLoggedIn;
